I am about to switch models or roles. Stop new implementation work and prepare a
compact, delta-first handoff under the project model-handoff rule. Update
MODEL_HANDOFF.md and STATUS.md; update the active plan/report only when their
owned facts changed. Point to exact file headings and evidence artifacts instead
of copying full plans, diffs, logs, or history. Make the active milestone and
exact next action identical in status and handoff. Record Git baseline, dirty-path
ownership, risks, stop conditions, live resources, and one requested response.
Package the largest cohesive execution batch that shares scope, invariants, and
acceptance gates; the exact next action is its restart pointer, not a micro-stage.
For execution-to-verification, include diff base, task-owned versus pre-existing paths,
every gate's command/exit/count/artifact, unrun gates with reasons, failures and
deviations, then request one named verification decision. Do not claim acceptance.
Use `Verification mode: self` and report that no model switch is needed only when the
capable implementer completed thin/standard work with all gates passing, no
material deviation or boundary change, and no independent gate. Otherwise route
to `independent` in a separate capable context or the exact specialized gate.
For `self`, the packet and helper check record one integrated closeout; they do not
create a model handoff or require the current context to reload the snapshot.
Finish or record owned work, run `python3 .model-handoff/handoff.py check .`, fix
all errors, leave a recoverable state, then stop.
