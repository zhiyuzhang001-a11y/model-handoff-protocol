# Changelog

## Unreleased

- Add protocol 0.5 risk-based review routing: every stage is reviewed, while
  eligible capable-model thin/standard work may use same-context `self` review;
  economical output, high-risk work, material deviations, and independent gates
  require a separate capable `inline` review.

- Add root-invariant behavioral-defect handoffs, adjacent adversarial variants,
  rejected shallow-fix records, and bounded implementer discretion over local
  mechanics; non-behavioral corrections may omit variants with a reason.
- Enforce actionable `REVIEW_TO_EXECUTE` correction fields while keeping local
  discretion inside allowed paths, dependency, interface, migration, and external
  write boundaries.
- Add a dependency-free live handoff checker and bounded bootstrap snapshot.
- Add staged context loading, exact evidence pointers, and compactness limits.
- Detect protocol/schema errors, stale Git claims, and status/handoff drift.
- Define risk-based planning depth so bounded implementers retain reversible local
  judgment without inheriting architecture or acceptance decisions.
- Add two minimal user switch phrases and a local, evidence-triggered improvement
  log that stays outside bootstrap context.
- Document safe promotion of target-project feedback: classify local versus
  generic issues, sanitize evidence, use a tested candidate branch, review before
  `main`, preview propagation, and require later live proof before resolution.
- Add protocol 0.3 `Review mode` routing so inline milestone review and optional
  Bugbot/Security gates are explicit, checker-enforced, and never require a user
  choice after switching.
- Define capability-aware routing: economical models perform most bounded work;
  capable models plan, independently review, and implement judgment-heavy or
  high-risk units without gaining broader authority.
- Require complete applicable invariant matrices and deterministic timing for
  high-risk corrections; a second post-fix failure returns to high-risk planning
  instead of receiving another case-specific patch.
- Add protocol 0.4 `Recommended capability` routing so the outgoing role tells
  the user whether the next implementer should be economical or capable; all
  planner/reviewer and high-risk handoffs require a capable model, and high-risk
  packets must carry a reasoned coverage matrix.
- Add a fixed-upstream, dependency-free on-demand update helper that records the
  fetched commit, requires no interactive credentials, defaults to
  preview, and preserves every existing `differs` or `conflict` for manual merge.
- Add one-sentence English and Chinese project-model requests for installing or
  updating the protocol from the public GitHub source as project rules.

## 0.1.0 — 2026-08-21

- Add concise always-applied model-handoff and evidence-first execution rules.
- Add a complete bidirectional planner/reviewer ↔ implementer playbook.
- Add blank handoff, status, and implementation-plan templates.
- Add outgoing, implementer, and reviewer prompts plus four sanitized examples.
- Add a dependency-free, dry-run-first, no-overwrite installer.
- Add privacy/structure validation, installer tests, and two-version CI.
- Add English and Chinese quick-start documentation, adoption/privacy checklists,
  contribution guidance, security policy, and Apache-2.0 licensing.
